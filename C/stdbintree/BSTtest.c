/*
  basic test program for the stdBSTree
*/
/* standard io */
#include <stdio.h>
/* mostly using NULL */
#include <stdlib.h>
/* well, we WILL need a tree */
#include "stdBSTree.h"

int main()
{

  /* create an empty tree */
  Tree tree; 
  /* create a NULL root */
  tNode* root = NULL;

  /* make the root the root */
  tree = &root;

  return 0;
}
